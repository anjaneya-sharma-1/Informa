# Core dependencies
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.24.0
plotly>=5.15.0
protobuf==4.23.4

# LangGraph and LangChain
langgraph>=0.5.0
langchain>=0.1.0
langchain-core>=0.1.0
langchain-text-splitters>=0.1.0
langsmith>=0.1.0

# Hugging Face and ML
transformers>=4.30.0
sentence-transformers>=2.2.0
torch>=2.0.0
tokenizers>=0.13.0
huggingface-hub>=0.19.0

# Vector Database
chromadb>=0.4.0
# Ensure modern SQLite (>=3.35) on hosts with old system sqlite
pysqlite3-binary>=0.5.2
# Optional alternative backend support
duckdb>=0.9.0

# Web scraping and HTTP
aiohttp>=3.8.0
requests>=2.31.0
beautifulsoup4>=4.12.0
newspaper3k>=0.2.8  # Optional: richer article content extraction
feedparser>=6.0.10

# HTML parsing / cleaning (fix ImportError: lxml.html.clean now separate project)
lxml>=5.2.0
lxml_html_clean>=0.2.0

# Async and utilities
pypi-json
regex
python-dotenv>=1.0.0
tqdm>=4.66.0

# Optional dependencies for enhanced functionality
psutil>=5.9.0  # For memory monitoring
